# Geracao de Temas

1. Ajustar paletas em `src/palettes/countries.ts`.
2. Ajustar regras compartilhadas em `src/theme/`.
3. Rodar `pnpm build`.
4. Abrir o tema no VS Code via VSIX.
5. Usar `samples/typescript/worldCupFixture.ts` para comparar tokens.
