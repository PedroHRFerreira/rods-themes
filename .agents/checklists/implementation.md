# Checklist de Implementacao

- Manter cada arquivo com no maximo 100 linhas.
- Preservar `editor.background` como `#000000`.
- Gerar todos os temas via `pnpm build`.
- Rodar `pnpm validate` antes de empacotar.
- Confirmar que `.agents/` e `AGENTS.md` seguem ignorados.
- Nao criar tema manual fora do gerador.
